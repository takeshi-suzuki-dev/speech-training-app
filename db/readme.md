# Database SQL

This directory contains SQL files for the Supabase PostgreSQL database.

## Directories

- `schema/`: Current database schema snapshots.
- `migrations/`: SQL changes applied to the database.
- `seeds/`: Initial or master data insert scripts.

## Notes

SQL files are applied manually through the Supabase SQL Editor for now.
A migration tool may be introduced in a later phase if needed.
